"""Baseline agents for comparison.

These are the agents Recall Lab is measured against. Each control implements
the same `Agent` protocol so the eval harness can run them through the same
conversation.
"""
